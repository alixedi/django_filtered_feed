============
Installation
============

At the command line::

    $ easy_install django_filtered_feed

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django_filtered_feed
    $ pip install django_filtered_feed